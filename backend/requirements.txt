# =============================================================================
# Restaurant Simulation Dashboard — Python Dependencies
# =============================================================================
# Direct dependencies only. Transitive dependencies (e.g., starlette, h11,
# anyio) are installed automatically by pip and should NOT be pinned here.
#
# To install: pip install -r requirements.txt
# To update:  pip install --upgrade -r requirements.txt
# =============================================================================

# --- Web framework & server ---
fastapi==0.128.1
uvicorn==0.40.0
gunicorn==23.0.0

# --- Database ORM ---
SQLAlchemy==2.0.46

# --- Data validation ---
pydantic==2.12.5

# --- Simulation engines ---
simpy==4.1.1

# --- AI features (OpenAI GPT-4o-mini) ---
openai>=1.30.0

# --- Environment variable loading ---
python-dotenv==1.2.1
